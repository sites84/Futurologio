name: Catalog cleanup

on:
  workflow_dispatch:

# Catalog generation is now owned by catalog-master-migrate.yml.
# This legacy workflow is intentionally disabled so it cannot overwrite
# the single master catalog with an older cleanup result.
jobs:
  cleanup:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy catalog cleanup disabled; master catalog workflow is authoritative.'
