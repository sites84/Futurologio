import fs from 'node:fs';
const file='backend/worker.js';
let s=fs.readFileSync(file,'utf8');
const marker="if(path==='/api/my-inventions'&&request.method==='GET')";
if(!s.includes("path==='/api/profile-settings'")){
  const route=`if(path==='/api/profile-settings'&&request.method==='GET'){if(!session)return json({ok:false,error:'Não autenticado'},401);const u=await user(env.DB,session.sub);if(!u)return json({ok:false,error:'Usuário não encontrado'},404);return json({ok:true,profile:{id:u.id,username:u.username,email:u.email,sex:u.sex||'',age:u.age==null?'':Number(u.age),address:u.address||'',bio:u.bio||'',avatar:u.avatar||'',profile_photo_url:u.profile_photo_key?\`\${url.origin}/api/profile-image/\${encodeURIComponent(u.id)}\`:null}})}
if(path==='/api/profile-settings'&&request.method==='POST'){if(!session)return json({ok:false,error:'Não autenticado'},401);const u=await user(env.DB,session.sub);if(!u)return json({ok:false,error:'Usuário não encontrado'},404);const b=await body(request);const username=String(b.username??u.username).trim(),sex=String(b.sex??u.sex??'').trim(),ageRaw=b.age===null||b.age===undefined||b.age===''?'':Number(b.age),address=String(b.address??u.address??'').trim(),bio=String(b.bio??u.bio??'').trim();if(!/^[a-zA-Z0-9_]{3,40}$/.test(username))return json({ok:false,error:'Nome de usuário inválido. Use 3 a 40 caracteres: letras, números ou _. '},400);if(ageRaw!==''&&(!Number.isInteger(ageRaw)||ageRaw<13||ageRaw>120))return json({ok:false,error:'Idade inválida. Informe uma idade entre 13 e 120 anos.'},400);if(sex&&!['masculino','feminino','outro','nao_informar'].includes(sex))return json({ok:false,error:'Opção de sexo inválida.'},400);if(address.length>300)return json({ok:false,error:'Endereço muito longo.'},400);if(bio.length>300)return json({ok:false,error:'Biografia muito longa.'},400);const clash=await env.DB.prepare('SELECT id FROM USERS WHERE username=? AND id<>?').bind(username,u.id).first();if(clash)return json({ok:false,error:'Nome de usuário já está em uso.'},409);await env.DB.prepare('UPDATE USERS SET username=?,sex=?,age=?,address=?,bio=?,updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(username,sex,ageRaw===''?null:ageRaw,address,bio,u.id).run();const v=await user(env.DB,u.id);return json({ok:true,user:pub(v,await allowance(env.DB,v)),profile:{id:v.id,username:v.username,email:v.email,sex:v.sex||'',age:v.age==null?'':Number(v.age),address:v.address||'',bio:v.bio||'',avatar:v.avatar||'',profile_photo_url:v.profile_photo_key?\`\${url.origin}/api/profile-image/\${encodeURIComponent(v.id)}\`:null}})}
if(path==='/api/profile-image'&&request.method==='POST'){if(!session)return json({ok:false,error:'Não autenticado'},401);if(!env.IMAGES)return json({ok:false,error:'Armazenamento de imagens indisponível.'},503);const form=await request.formData(),file=form.get('image');if(!file||typeof file.arrayBuffer!=='function')return json({ok:false,error:'Imagem inválida.'},400);const type=String(file.type||'').toLowerCase();if(!['image/jpeg','image/png','image/webp','image/gif'].includes(type))return json({ok:false,error:'Use JPG, PNG, WebP ou GIF.'},400);if(file.size>5*1024*1024)return json({ok:false,error:'A foto de perfil deve ter no máximo 5 MB.'},413);const u=await user(env.DB,session.sub);if(!u)return json({ok:false,error:'Usuário não encontrado'},404);const ext=({ 'image/jpeg':'jpg','image/png':'png','image/webp':'webp','image/gif':'gif'})[type];const key=\`profiles/\${u.id}/\${crypto.randomUUID()}.\${ext}\`;await env.IMAGES.put(key,file,{httpMetadata:{contentType:type,cacheControl:'public,max-age=31536000,immutable'}});if(u.profile_photo_key)await env.IMAGES.delete(u.profile_photo_key);await env.DB.prepare('UPDATE USERS SET profile_photo_key=?,profile_photo_content_type=?,updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(key,type,u.id).run();return json({ok:true,profile_photo_url:\`\${url.origin}/api/profile-image/\${encodeURIComponent(u.id)}\`})}
if(path==='/api/profile-image/delete'&&request.method==='POST'){if(!session)return json({ok:false,error:'Não autenticado'},401);const u=await user(env.DB,session.sub);if(!u)return json({ok:false,error:'Usuário não encontrado'},404);if(u.profile_photo_key&&env.IMAGES)await env.IMAGES.delete(u.profile_photo_key);await env.DB.prepare('UPDATE USERS SET profile_photo_key=NULL,profile_photo_content_type=NULL,updated_at=CURRENT_TIMESTAMP WHERE id=?').bind(u.id).run();return json({ok:true})}
if(path.startsWith('/api/profile-image/')&&request.method==='GET'){if(!env.IMAGES)return new Response('Not configured',{status:503});const id=decodeURIComponent(path.split('/').pop()||'');const u=await env.DB.prepare('SELECT profile_photo_key FROM USERS WHERE id=?').bind(id).first();if(!u?.profile_photo_key)return new Response('Not found',{status:404});const object=await env.IMAGES.get(u.profile_photo_key);if(!object)return new Response('Not found',{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set('cache-control','public,max-age=31536000,immutable');headers.set('access-control-allow-origin','*');return new Response(object.body,{headers})}
`;
  const at=s.indexOf(marker);
  if(at<0)throw new Error('Profile route marker not found');
  s=s.slice(0,at)+route+s.slice(at);
}
fs.writeFileSync(file,s);
console.log('Profile routes patched.');
